<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.machine.service.data.tag.dao.mapper.DataTagCategoryMapper">

    <!-- 根据父ID查询子分类列表 -->
    <select id="selectByParentId" resultType="com.machine.service.data.tag.dao.mapper.entity.DataTagCategoryEntity">
        SELECT id, parent_id, code, name, sort, create_by, create_time, update_by, update_time
        FROM t_data_tag_category
        WHERE parent_id = #{parentId}
        ORDER BY sort DESC, create_time ASC
    </select>

    <!-- 根据编码查询分类 -->
    <select id="selectByCode" resultType="com.machine.service.data.tag.dao.mapper.entity.DataTagCategoryEntity">
        SELECT id, parent_id, code, name, sort, create_by, create_time, update_by, update_time
        FROM t_data_tag_category
        WHERE code = #{code}
        LIMIT 1
    </select>

    <!-- 查询所有分类 -->
    <select id="selectAll" resultType="com.machine.service.data.tag.dao.mapper.entity.DataTagCategoryEntity">
        SELECT id, parent_id, code, name, sort, create_by, create_time, update_by, update_time
        FROM t_data_tag_category
        ORDER BY sort DESC, create_time ASC
    </select>

</mapper>
